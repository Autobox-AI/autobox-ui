'use client'

// Direct recharts imports to avoid lazy loading issues with complex props
export {
  LineChart,
  BarChart,
  Bar,
  ResponsiveContainer,
  CartesianGrid,
  Line,
  XAxis,
  YAxis,
  Tooltip,
  Tooltip as RechartsTooltip,
} from 'recharts'
