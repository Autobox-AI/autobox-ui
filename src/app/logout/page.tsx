import { UnderConstruction } from '@/components/under-construction'

export default function LogoutPage() {
  return (
    <UnderConstruction
      pageName="Logout"
      description="Secure session management is being implemented. Soon you'll have a proper logout flow with session cleanup, device management, and the option to sign out from all devices. We're ensuring your data remains secure with proper authentication state handling and session termination."
    />
  )
}
